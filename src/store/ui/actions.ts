import { User } from '../user/types'

// export const handleError = (err) => ({
//   type: AT.ERROR_UI,
//   payload: err
// })

// export const handleBackdrop = (toggle) => ({
//   type: AT.BACKDROP_UI,
//   payload: toggle
// })
